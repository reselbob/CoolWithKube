---
kind: Service
apiVersion: v1
metadata:
  name: feelingtracker
spec:
  selector:
    app: feelingtracker
  type: NodePort
  ports:
    - name: main
      protocol: TCP
      port: 80
      targetPort: 3000

